/* === HTMHELLPAD — Configuration === */

var CONFIG = {
    // Operator name shown in boot sequence
    HELLDIVER_NAME: 'HELLDIVER [UNREGISTERED]',

    // Super Destroyer ship name
    SHIP_NAME: 'Star of Democracy',

    // Flip portrait rotation 180 degrees
    FLIP_ORIENTATION: false,

    // Boot sequence duration in ms
    BOOT_DURATION: 7000,

    // Error flash duration in ms
    ERROR_DURATION: 1000,

    // Idle timeout before screensaver in ms
    IDLE_TIMEOUT: 10000,

    // Stratagem progress bar duration in ms
    STRATAGEM_DURATION: 3500,

    // Delay before stratagem voiceline plays in ms
    VOICELINE_DELAY: 800,

    // Skip boot sequence and go straight to welcome screen
    SKIP_BOOT: false,

    // Auto-dismiss stratagem screen 1s after progress completes
    AUTO_DISMISS_STRATAGEM: false,
};
